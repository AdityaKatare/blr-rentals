{
  "name": "@blr/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PostgreSQL/PostGIS migrations, seeds, queries and the DB client shared by web and scraper.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "migrate": "tsx src/scripts/migrate.ts",
    "seed": "tsx src/scripts/seed.ts",
    "build-metro-seed": "tsx src/scripts/build-metro-seed.ts"
  },
  "dependencies": {
    "@blr/core": "workspace:*",
    "dotenv": "^16.4.5",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
