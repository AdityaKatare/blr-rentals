{
  "name": "@blr/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared contracts: NormalizedListing, SearchQuery, enums, normalizers, ranking and dedup scoring.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
